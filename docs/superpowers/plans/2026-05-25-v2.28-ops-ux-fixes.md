# v2.28 Ops UX Fixes 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 一次性 ship 10 个 P1 改进，覆盖 3 模块（执行控制 / 账号管理 / 代理）+ 后端 schema migration + engine 注入。

**Architecture:** 后端先加 schema 列 + engine 在 emitStatus 时从 proxyMgr 注入代理上下文；前端再消费这些字段并重组 UI。`status.js` 加 3 个新导出（`FAILED_TO_RETRY_STATUSES` / `isFailedToRetry` / `EXECUTE_STATUS_FILTER_OPTIONS`）作为 Execute/Accounts 共用工具。Config.vue 由 482 行平铺改为 7 个 `el-tabs`。

**Tech Stack:** Vue 3 + Element Plus / Node `node:test` / sql.js (SQLite) / Express.

**Spec:** `docs/superpowers/specs/2026-05-25-v2.28-ops-ux-fixes-design.md`

---

## 文件清单

**新建：**
- `__tests__/status-filter-options.test.js` — EXECUTE_STATUS_FILTER_OPTIONS 单测
- `__tests__/db-status-proxy-cols.test.js` — schema migration 单测

**修改：**
- `server/db.js` — account_status 加 proxy_node / exit_ip 列 + PRAGMA-gated ALTER + statusDB.set merge 扩展
- `server/engine.js` — emitStatus 注入代理上下文
- `protocol-engine.js` — 同上
- `server/routes/results.js` — 透传 proxyNode / exitIp 到响应
- `web/src/status.js` — 加 3 个新导出
- `__tests__/status-groups.test.js` — 追加 G11 / G12 用例
- `web/src/views/Execute.vue` — #1/#2/#3/#4/#5 五项改动
- `web/src/components/AccountTableRows.vue` — #3 reason 列 + expand-row banner
- `web/src/views/Accounts.vue` — #6/#7/#8
- `web/src/views/Config.vue` — #9/#10
- `docs/CHANGELOG.md` — 追加 v2.28 节

---

## Task 1：DB schema migration + statusDB.set 扩展（TDD M1-M3）

**Files:**
- Modify: `server/db.js`
- Create: `__tests__/db-status-proxy-cols.test.js`

### Step 1：写失败测试

- [ ] **Step 1:** Create `__tests__/db-status-proxy-cols.test.js`:

```js
const test = require('node:test')
const assert = require('node:assert')
const initSqlJs = require('sql.js')

let SQL

test.before(async () => { SQL = await initSqlJs() })

test('M1 新装库 account_status CREATE 含 proxy_node / exit_ip 列', () => {
  const db = new SQL.Database()
  // 复现 db.js initDB 的 CREATE TABLE 块（spec §Data Structures）
  db.run(`
    CREATE TABLE IF NOT EXISTS account_status (
      email TEXT PRIMARY KEY,
      status TEXT DEFAULT 'idle',
      proxy_node TEXT DEFAULT '',
      exit_ip TEXT DEFAULT ''
    );
  `)
  const cols = db.exec("PRAGMA table_info(account_status)")
  const names = new Set(cols[0].values.map(v => v[1]))
  assert.ok(names.has('proxy_node'))
  assert.ok(names.has('exit_ip'))
})

test('M2 老库无 proxy_node 时 PRAGMA-gated ALTER 添加成功', () => {
  const db = new SQL.Database()
  db.run(`CREATE TABLE account_status (email TEXT PRIMARY KEY, status TEXT)`)
  const cols = db.exec("PRAGMA table_info(account_status)")
  const names = new Set(cols[0].values.map(v => v[1]))
  if (!names.has('proxy_node')) {
    db.run("ALTER TABLE account_status ADD COLUMN proxy_node TEXT DEFAULT ''")
  }
  if (!names.has('exit_ip')) {
    db.run("ALTER TABLE account_status ADD COLUMN exit_ip TEXT DEFAULT ''")
  }
  const cols2 = db.exec("PRAGMA table_info(account_status)")
  const names2 = new Set(cols2[0].values.map(v => v[1]))
  assert.ok(names2.has('proxy_node'))
  assert.ok(names2.has('exit_ip'))
})

test('M3 已含 proxy_node 列时跳过 ALTER（幂等不抛错）', () => {
  const db = new SQL.Database()
  db.run(`CREATE TABLE account_status (email TEXT PRIMARY KEY, proxy_node TEXT, exit_ip TEXT)`)
  const cols = db.exec("PRAGMA table_info(account_status)")
  const names = new Set(cols[0].values.map(v => v[1]))
  if (!names.has('proxy_node')) {
    db.run("ALTER TABLE account_status ADD COLUMN proxy_node TEXT DEFAULT ''")
  }
  if (!names.has('exit_ip')) {
    db.run("ALTER TABLE account_status ADD COLUMN exit_ip TEXT DEFAULT ''")
  }
  // 不抛即通过
  assert.ok(true)
})
```

### Step 2：跑测试确认失败

- [ ] **Step 2:** Run: `node --test __tests__/db-status-proxy-cols.test.js`

Expected: 3 tests PASS（这些是纯 sql.js 行为，不依赖 db.js 改动；本测试主要锁定 PRAGMA-gated ALTER 模式 — 后续 db.js 的迁移代码必须复用此模式）

### Step 3：修改 db.js CREATE TABLE 块加 2 列

- [ ] **Step 3:** Open `server/db.js`. Find the `CREATE TABLE IF NOT EXISTS account_status` block. Currently it has these columns: `email, status, phase, progress, reason, has_auth_file, updated_at` plus payment_link/alive_*/last_access_token columns added via separate `ALTER` blocks lower.

Find this block (around lines 30-50 in initDB function):

```js
    CREATE TABLE IF NOT EXISTS account_status (
      email TEXT PRIMARY KEY,
      status TEXT DEFAULT 'idle',
      phase TEXT DEFAULT '',
      progress TEXT DEFAULT '',
      reason TEXT DEFAULT '',
      has_auth_file INTEGER DEFAULT 0,
      updated_at TEXT DEFAULT (datetime('now'))
    );
```

Replace with (加 2 列 `proxy_node` / `exit_ip`):

```js
    CREATE TABLE IF NOT EXISTS account_status (
      email TEXT PRIMARY KEY,
      status TEXT DEFAULT 'idle',
      phase TEXT DEFAULT '',
      progress TEXT DEFAULT '',
      reason TEXT DEFAULT '',
      has_auth_file INTEGER DEFAULT 0,
      updated_at TEXT DEFAULT (datetime('now')),
      proxy_node TEXT DEFAULT '',
      exit_ip TEXT DEFAULT ''
    );
```

### Step 4：在 ALTER 块中追加防御性迁移

- [ ] **Step 4:** Find the existing `existingCols` check block (around lines 80-111). It currently checks `payment_link / payment_link_pk / payment_link_at / alive_status / alive_checked_at / alive_reason / last_access_token / last_session_json / last_access_token_at`. Add `proxy_node` and `exit_ip` to the same pattern.

After the existing line:

```js
  if (!existingCols.has('last_access_token_at')) {
    db.run("ALTER TABLE account_status ADD COLUMN last_access_token_at TEXT DEFAULT ''");
  }
```

Insert IMMEDIATELY AFTER:

```js
  if (!existingCols.has('proxy_node')) {
    db.run("ALTER TABLE account_status ADD COLUMN proxy_node TEXT DEFAULT ''");
  }
  if (!existingCols.has('exit_ip')) {
    db.run("ALTER TABLE account_status ADD COLUMN exit_ip TEXT DEFAULT ''");
  }
```

### Step 5：扩展 statusDB.set merge-aware 处理 proxy_node / exit_ip

- [ ] **Step 5:** Find `statusDB.set` (around lines 168-212). Currently merges `payment_link*` / `last_access_token*` with explicit `'paymentLink' in incoming` checks. Add the same pattern for `proxyNode` / `exitIp` camelCase incoming → snake_case columns.

Find this block (around lines 196-198):

```js
    const last_access_token_at = ('accessToken' in incoming && incoming.accessToken)
      ? new Date().toISOString()
      : (existing.last_access_token_at || '');
```

Insert IMMEDIATELY AFTER:

```js
    // proxy_node / exit_ip — only override when caller explicitly passed the key
    const proxy_node = 'proxyNode' in incoming
      ? (incoming.proxyNode || '')
      : (existing.proxy_node || '');
    const exit_ip = 'exitIp' in incoming
      ? (incoming.exitIp || '')
      : (existing.exit_ip || '');
```

Then find the `db.run(` INSERT statement immediately after (around lines 204-210):

```js
    db.run(
      "INSERT OR REPLACE INTO account_status (email, status, phase, progress, reason, has_auth_file, payment_link, payment_link_pk, payment_link_at, alive_status, alive_checked_at, alive_reason, last_access_token, last_session_json, last_access_token_at, updated_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,datetime('now'))",
      [email, status, phase, progress || '', reason || '', has_auth_file ? 1 : 0,
       payment_link, payment_link_pk, payment_link_at,
       existingAlive.alive_status, existingAlive.alive_checked_at, existingAlive.alive_reason,
       last_access_token, last_session_json, last_access_token_at]
    );
```

Replace with (在 INSERT 列表和 values 数组各加 `proxy_node, exit_ip`):

```js
    db.run(
      "INSERT OR REPLACE INTO account_status (email, status, phase, progress, reason, has_auth_file, payment_link, payment_link_pk, payment_link_at, alive_status, alive_checked_at, alive_reason, last_access_token, last_session_json, last_access_token_at, proxy_node, exit_ip, updated_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,datetime('now'))",
      [email, status, phase, progress || '', reason || '', has_auth_file ? 1 : 0,
       payment_link, payment_link_pk, payment_link_at,
       existingAlive.alive_status, existingAlive.alive_checked_at, existingAlive.alive_reason,
       last_access_token, last_session_json, last_access_token_at,
       proxy_node, exit_ip]
    );
```

### Step 6：跑全套测试确认无回归

- [ ] **Step 6:**

Run:
```bash
node --test __tests__/*.test.js server/__tests__/*.test.js server/proxy/__tests__/*.test.js server/liveness/__tests__/*.test.js
```

Expected: ALL PASS（既有 ~184 + 新 3 = ~187 个）。

### Step 7：Commit

- [ ] **Step 7:**

```bash
git add server/db.js __tests__/db-status-proxy-cols.test.js
git commit -m "$(cat <<'EOF'
feat(db): account_status 加 proxy_node / exit_ip 列 + merge-aware set

account_status 表新增 proxy_node / exit_ip 两列。冷启动 CREATE TABLE
块同步加；老库通过 PRAGMA-gated ALTER 防御性迁移（与既有
alive_status / last_access_token 等列同 pattern，幂等不抛错）。

statusDB.set 沿用 'in incoming' 显式判断模式（与 paymentLink /
accessToken 同语义）：调用方传 proxyNode / exitIp camelCase 才
更新对应列，缺失时保留既有值。critical invariant — 中间态
emitStatus 不会清空已写入的代理上下文。

3 单测覆盖 CREATE 含新列 / PRAGMA-gated ALTER / 幂等不抛。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2：Engine emitStatus 注入代理上下文 + results.js 透传

**Files:**
- Modify: `server/engine.js`
- Modify: `protocol-engine.js`
- Modify: `server/routes/results.js`

### Step 1：engine.js emitStatus 注入

- [ ] **Step 1:** In `server/engine.js`, find the `emitStatus` method (around line 52):

```js
  emitStatus(data) {
    this.emit('account-status', data);
    try { getDB().statusDB.set(data.email, data); } catch (e) { console.log(`[WARN] statusDB.set: ${e.message?.slice(0, 60)}`); }
  }
```

Replace with (从 proxyMgr 读 currentNode + exitIp 注入；不存在或 throw 时传空值):

```js
  emitStatus(data) {
    // 从 proxyMgr 注入代理上下文。merge-aware statusDB.set 在缺失时保留旧值；
    // 这里始终注入（除非完全取不到状态），覆盖刚发生的代理切换。
    try {
      const proxyMgr = require('./proxy');
      const st = proxyMgr.getState();
      data = { ...data, proxyNode: st.currentNode || '', exitIp: st.exitIp || '' };
    } catch {}
    this.emit('account-status', data);
    try { getDB().statusDB.set(data.email, data); } catch (e) { console.log(`[WARN] statusDB.set: ${e.message?.slice(0, 60)}`); }
  }
```

### Step 2：protocol-engine.js emitStatus 同改

- [ ] **Step 2:** In `protocol-engine.js`, find the `emitStatus` method (around line 104):

```js
  emitStatus(data) {
    this.emit('account-status', data);
    try {
      const { statusDB } = require('./server/db');
      statusDB.set(data.email, data);
    } catch (e) { console.log(`[WARN] statusDB.set: ${e.message?.slice(0, 60)}`); }
  }
```

Replace with:

```js
  emitStatus(data) {
    try {
      const proxyMgr = require('./server/proxy');
      const st = proxyMgr.getState();
      data = { ...data, proxyNode: st.currentNode || '', exitIp: st.exitIp || '' };
    } catch {}
    this.emit('account-status', data);
    try {
      const { statusDB } = require('./server/db');
      statusDB.set(data.email, data);
    } catch (e) { console.log(`[WARN] statusDB.set: ${e.message?.slice(0, 60)}`); }
  }
```

### Step 3：routes/results.js 透传 proxyNode / exitIp

- [ ] **Step 3:** In `server/routes/results.js`, find the `GET /` handler (around line 16-33):

```js
router.get('/', (req, res) => {
  try {
    const statuses = statusDB.list();
    const results = statuses.map(s => ({
      email: s.email,
      status: s.status,
      phase: s.phase,
      progress: s.progress,
      reason: s.reason,
      hasAuthFile: s.has_auth_file === 1 || fs.existsSync(path.join(CPA_AUTH_DIR, emailToAuthFilename(s.email, 'cpa'))) || fs.existsSync(path.join(CPA_AUTH_DIR, emailToAuthFilename(s.email, 'sub2api'))),
      updatedAt: s.updated_at,
      alive_status: s.alive_status || 'unknown',
      alive_checked_at: s.alive_checked_at || '',
      alive_reason: s.alive_reason || '',
    }));
    res.json(results);
  } catch (err) { res.status(500).json({ error: err.message }); }
});
```

Replace with (加 `proxyNode` + `exitIp` 两行):

```js
router.get('/', (req, res) => {
  try {
    const statuses = statusDB.list();
    const results = statuses.map(s => ({
      email: s.email,
      status: s.status,
      phase: s.phase,
      progress: s.progress,
      reason: s.reason,
      hasAuthFile: s.has_auth_file === 1 || fs.existsSync(path.join(CPA_AUTH_DIR, emailToAuthFilename(s.email, 'cpa'))) || fs.existsSync(path.join(CPA_AUTH_DIR, emailToAuthFilename(s.email, 'sub2api'))),
      updatedAt: s.updated_at,
      proxyNode: s.proxy_node || '',
      exitIp: s.exit_ip || '',
      alive_status: s.alive_status || 'unknown',
      alive_checked_at: s.alive_checked_at || '',
      alive_reason: s.alive_reason || '',
    }));
    res.json(results);
  } catch (err) { res.status(500).json({ error: err.message }); }
});
```

### Step 4：手动 smoke test

- [ ] **Step 4:** Start the server and verify the new fields appear:

```bash
node server/index.js &
# Wait 3 sec then:
curl -s http://localhost:3000/api/results | head -c 500
```

Expected: 响应 JSON 含 `proxyNode` 和 `exitIp` 字段（值可能为空字符串，因为还没执行过账号）。停止 server。

### Step 5：跑全测试无回归

- [ ] **Step 5:**

```bash
node --test __tests__/*.test.js server/__tests__/*.test.js server/proxy/__tests__/*.test.js server/liveness/__tests__/*.test.js
```

Expected: 187/187 PASS（与 Task 1 末同；本任务未加测试）

### Step 6：Commit

- [ ] **Step 6:**

```bash
git add server/engine.js protocol-engine.js server/routes/results.js
git commit -m "$(cat <<'EOF'
feat(engine): emitStatus 注入 proxyNode / exitIp + results 透传

server/engine.js 和 protocol-engine.js 在每次 emitStatus 时
从 proxyMgr.getState() 读 currentNode + exitIp 注入 data，
经 statusDB.set merge-aware 写入 account_status 表对应列。

server/routes/results.js GET / 响应加 proxyNode / exitIp 两个
camelCase 字段（snake_case 来自 DB）。

外层 try/catch 保护 proxyMgr 未 ready 的边界（如冷启动初期）。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3：status.js 加 3 个新导出（TDD G11/G12/S1）

**Files:**
- Modify: `web/src/status.js`
- Modify: `__tests__/status-groups.test.js`
- Create: `__tests__/status-filter-options.test.js`

### Step 1：追加 G11 / G12 到 status-groups.test.js

- [ ] **Step 1:** Open `__tests__/status-groups.test.js`. Append at end:

```js

// === Task 3: isFailedToRetry helper ===

let isFailedToRetry

test.before(async () => {
  const mod = await import('../web/src/status.js')
  isFailedToRetry = mod.isFailedToRetry
})

test('G11 isFailedToRetry 覆盖所有 9 个未拿到 Plus 的终态', () => {
  for (const s of ['error', 'no_link', 'no_promo', 'verify_error', 'paypal_captcha',
                   'login_fail', 'token_expired', 'aborted', 'no_jp_proxy']) {
    assert.strictEqual(isFailedToRetry(s), true, `${s} 应当可重试`)
  }
})

test('G12 isFailedToRetry 排除终态 plus / plus_no_rt / deactivated / idle / running / 空值', () => {
  for (const s of ['plus', 'plus_no_rt', 'deactivated', 'idle', 'running', 'checking', '', undefined, null]) {
    assert.strictEqual(isFailedToRetry(s), false, `${JSON.stringify(s)} 不应可重试`)
  }
})
```

### Step 2：创建 status-filter-options.test.js

- [ ] **Step 2:** Create `__tests__/status-filter-options.test.js`:

```js
const test = require('node:test')
const assert = require('node:assert')

let EXECUTE_STATUS_FILTER_OPTIONS

test.before(async () => {
  const mod = await import('../web/src/status.js')
  EXECUTE_STATUS_FILTER_OPTIONS = mod.EXECUTE_STATUS_FILTER_OPTIONS
})

test('S1 EXECUTE_STATUS_FILTER_OPTIONS 含所有 LABEL_MAP 状态除 checking / canceled', () => {
  const values = EXECUTE_STATUS_FILTER_OPTIONS.map(o => o.value)
  // 必须包含的（4 个新增的，原 Execute.vue 硬编码下拉缺失）
  for (const s of ['paypal_captcha', 'login_fail', 'token_expired', 'aborted']) {
    assert.ok(values.includes(s), `${s} 应在下拉中`)
  }
  // 必须包含的（旧 11 个）
  for (const s of ['plus', 'plus_no_rt', 'error', 'deactivated', 'no_link', 'idle', 'running', 'no_jp_proxy', 'no_promo', 'verify_error']) {
    assert.ok(values.includes(s), `${s} 应在下拉中`)
  }
  // 必须排除的（纯 liveness 维度）
  assert.ok(!values.includes('checking'), 'checking 不应在下拉中')
  assert.ok(!values.includes('canceled'), 'canceled 不应在下拉中')
  // 每个 option 有 label + value
  for (const opt of EXECUTE_STATUS_FILTER_OPTIONS) {
    assert.ok(typeof opt.value === 'string' && opt.value, `option.value 必须为非空 string`)
    assert.ok(typeof opt.label === 'string' && opt.label, `option.label 必须为非空 string`)
  }
})
```

### Step 3：跑测试确认失败

- [ ] **Step 3:**

```bash
node --test __tests__/status-groups.test.js __tests__/status-filter-options.test.js
```

Expected: G11 / G12 / S1 FAIL（缺导出）。其他 G1-G10 PASS。

### Step 4：在 status.js 末尾追加 3 个新导出

- [ ] **Step 4:** Open `web/src/status.js`. Find the LAST line of the file (currently likely `export const DEFAULT_EXPANDED_STATUSES = ['plus', 'plus_no_rt']` or similar, after `groupAccountsByStatus`). Append at end:

```js

// === Execute page retry / filter helpers ===

// 所有 "未拿到 Plus 但非账号死亡" 的终态 — "重试失败" 按钮的目标集
// 排除 deactivated（账号已删）、plus / plus_no_rt（已成功）、
// idle / running（未到终态）、checking / unknown（liveness 中间态）
export const FAILED_TO_RETRY_STATUSES = [
  'error',
  'no_link',
  'no_promo',
  'verify_error',
  'paypal_captcha',
  'login_fail',
  'token_expired',
  'aborted',
  'no_jp_proxy',
]

export function isFailedToRetry(status) {
  return FAILED_TO_RETRY_STATUSES.includes((status || '').toLowerCase())
}

// statusFilter 下拉的动态选项 — 从 LABEL_MAP 全集生成，避免下拉与状态体系脱节
// 排除纯 liveness 维度（checking / canceled），它们不在执行流水线 status
export const EXECUTE_STATUS_FILTER_OPTIONS = Object.entries(LABEL_MAP)
  .filter(([k]) => !['checking', 'canceled'].includes(k))
  .map(([value, label]) => ({ value, label }))
```

### Step 5：跑测试确认通过

- [ ] **Step 5:**

```bash
node --test __tests__/status-groups.test.js __tests__/status-filter-options.test.js
```

Expected: 所有 PASS（G1-G12 + S1 = 13 cases）

### Step 6：跑全套测试无回归

- [ ] **Step 6:**

```bash
node --test __tests__/*.test.js server/__tests__/*.test.js server/proxy/__tests__/*.test.js server/liveness/__tests__/*.test.js
```

Expected: ALL PASS（187 + 3 新 = 190）

### Step 7：Commit

- [ ] **Step 7:**

```bash
git add web/src/status.js __tests__/status-groups.test.js __tests__/status-filter-options.test.js
git commit -m "$(cat <<'EOF'
feat(status): FAILED_TO_RETRY_STATUSES + EXECUTE_STATUS_FILTER_OPTIONS

新增 3 个导出供 Execute / Accounts 共用：
- FAILED_TO_RETRY_STATUSES (9 项)：所有未拿到 Plus 但非死号的终态
- isFailedToRetry(status)：单值判定 helper
- EXECUTE_STATUS_FILTER_OPTIONS：从 LABEL_MAP 动态生成的下拉选项
  （排除 checking / canceled 这两个纯 liveness 维度）

之所以独立于 ERROR_STATUSES：后者含 deactivated 但缺 5 个状态；
语义也不同（"重试" ≠ "错误"）。

3 单测：G11/G12 isFailedToRetry 正反例 + S1 下拉选项完整性。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4：Execute.vue + AccountTableRows.vue 改造（#1/#2/#3/#4/#5）

**Files:**
- Modify: `web/src/views/Execute.vue`
- Modify: `web/src/components/AccountTableRows.vue`

### Step 1：Execute.vue imports 扩展

- [ ] **Step 1:** Open `web/src/views/Execute.vue`. Find existing imports (around line 1-10 of `<script setup>`):

```js
import { ref, computed, watch, onMounted, shallowRef, nextTick } from 'vue'
import { ElMessage } from 'element-plus'
import api from '../api'
import { socketState } from '../socket'
import { statusType, statusLabel, PLUS_STATUSES, DEFAULT_EXPANDED_STATUSES, groupAccountsByStatus } from '../status'
import { getSelectionSet, clearSelection } from '../selection'
import AccountTableRows from '../components/AccountTableRows.vue'
```

Replace with (加 `onBeforeUnmount`、`isFailedToRetry`、`EXECUTE_STATUS_FILTER_OPTIONS`):

```js
import { ref, computed, watch, onMounted, onBeforeUnmount, shallowRef, nextTick } from 'vue'
import { ElMessage } from 'element-plus'
import api from '../api'
import { socketState } from '../socket'
import { statusType, statusLabel, PLUS_STATUSES, DEFAULT_EXPANDED_STATUSES, groupAccountsByStatus, isFailedToRetry, EXECUTE_STATUS_FILTER_OPTIONS } from '../status'
import { getSelectionSet, clearSelection } from '../selection'
import AccountTableRows from '../components/AccountTableRows.vue'
```

### Step 2：#1 替换 failedEmails computed

- [ ] **Step 2:** Find:

```js
const failedEmails = computed(() => accounts.value.filter(a => a._status === 'error').map(a => a.email))
```

Replace with:

```js
const failedEmails = computed(() => accounts.value.filter(a => isFailedToRetry(a._status)).map(a => a.email))
```

### Step 3：#2 替换 statusFilter 下拉模板

- [ ] **Step 3:** Find the `<el-select v-model="statusFilter"` block. It will look like:

```vue
        <el-select v-model="statusFilter" placeholder="状态" clearable style="width:130px;margin-left:8px">
          <el-option label="Plus(有RT)" value="plus" />
          <el-option label="Plus(无RT)" value="plus_no_rt" />
          <el-option label="错误" value="error" />
          <el-option label="已删除" value="deactivated" />
          <el-option label="无链接" value="no_link" />
          <el-option label="空闲" value="idle" />
          <el-option label="运行中" value="running" />
          <el-option label="已停止" value="aborted" />
          <el-option label="JP节点不可用" value="no_jp_proxy" />
          <el-option label="无0元资格" value="no_promo" />
          <el-option label="Stripe验证失败" value="verify_error" />
        </el-select>
```

Replace with:

```vue
        <el-select v-model="statusFilter" placeholder="状态" clearable style="width:130px;margin-left:8px">
          <el-option v-for="opt in EXECUTE_STATUS_FILTER_OPTIONS" :key="opt.value" :label="opt.label" :value="opt.value" />
        </el-select>
```

### Step 4：#4 添加 engineMode state + loadEngineMode 函数

- [ ] **Step 4:** In `<script setup>`, find an early ref declaration block (after `const accounts = ref([])` or similar). Insert this state:

```js
const engineMode = ref({ protocolMode: false, paymentLinkSource: 'api' })

async function loadEngineMode() {
  try {
    const { data } = await api.get('/config/raw')
    const cfg = data.config || data
    engineMode.value = {
      protocolMode: !!cfg.protocolMode,
      paymentLinkSource: cfg.paymentLinkSource || 'api',
    }
  } catch {}
}
```

### Step 5：#4 toolbar 加 mode badges

- [ ] **Step 5:** In template, find the toolbar's `<el-tag :type="running ? 'warning' : 'info'">{{ running ? '运行中' : '空闲' }}</el-tag>` line. After it, insert:

```vue
        <el-divider direction="vertical" />
        <el-tag :type="engineMode.protocolMode ? 'success' : 'warning'" size="default">
          {{ engineMode.protocolMode ? '协议模式' : '浏览器模式' }}
        </el-tag>
        <el-tag type="info" size="default" style="margin-left:6px">
          link: {{ engineMode.paymentLinkSource === 'api' ? 'API' : 'Discord' }}
        </el-tag>
```

### Step 6：#5 添加 polling 函数

- [ ] **Step 6:** In `<script setup>`, after the `loadEngineMode` function added in Step 4, insert:

```js
let statusPollTimer = null

function startPolling() {
  const tick = async () => {
    if (document.visibilityState !== 'visible') return
    try {
      const { data } = await api.get('/execute/status')
      running.value = data.status === 'running'
    } catch {}
  }
  statusPollTimer = setInterval(tick, 5000)
  document.addEventListener('visibilitychange', tick)
}

onBeforeUnmount(() => {
  if (statusPollTimer) {
    clearInterval(statusPollTimer)
    statusPollTimer = null
  }
})
```

### Step 7：#4/#5 改 onMounted 调 loadEngineMode + startPolling

- [ ] **Step 7:** Find:

```js
onMounted(() => { loadAccounts(); checkStatus() })
```

Replace with:

```js
onMounted(() => { loadAccounts(); checkStatus(); loadEngineMode(); startPolling() })
```

### Step 8：#3 loadResults 透传 _reason / _proxyNode / _exitIp / _updatedAt

- [ ] **Step 8:** Find `loadResults` function:

```js
async function loadResults() {
  try {
    const { data } = await api.get('/results')
    for (const r of data) {
      const row = accounts.value.find(a => a.email === r.email)
      if (row) {
        if (r.status && r.status !== 'idle') row._status = r.status
        if (r.phase) row._phase = r.phase
        row._hasAuth = r.hasAuthFile || false
        const st = (r.status || '').toLowerCase()
        row._plan = PLUS_STATUSES.includes(st) ? 'plus' : (['error', 'no_link'].includes(st) ? 'free' : '')
      }
    }
  } catch {}
}
```

Replace with (加 4 个新字段):

```js
async function loadResults() {
  try {
    const { data } = await api.get('/results')
    for (const r of data) {
      const row = accounts.value.find(a => a.email === r.email)
      if (row) {
        if (r.status && r.status !== 'idle') row._status = r.status
        if (r.phase) row._phase = r.phase
        row._hasAuth = r.hasAuthFile || false
        row._reason = r.reason || ''
        row._proxyNode = r.proxyNode || ''
        row._exitIp = r.exitIp || ''
        row._updatedAt = r.updatedAt || ''
        const st = (r.status || '').toLowerCase()
        row._plan = PLUS_STATUSES.includes(st) ? 'plus' : (['error', 'no_link'].includes(st) ? 'free' : '')
      }
    }
  } catch {}
}
```

### Step 9：#3 loadAccounts 初始化 row 默认空值

- [ ] **Step 9:** Find `loadAccounts` function:

```js
async function loadAccounts() {
  try {
    const { data } = await api.get('/accounts')
    accounts.value = data.map(a => ({ ...a, _status: 'idle', _phase: '', _hasAuth: false, _showHistory: false, _plan: '' }))
    await loadResults()
  } catch {}
}
```

Replace with (init 时也加 4 字段空值):

```js
async function loadAccounts() {
  try {
    const { data } = await api.get('/accounts')
    accounts.value = data.map(a => ({ ...a, _status: 'idle', _phase: '', _hasAuth: false, _showHistory: false, _plan: '', _reason: '', _proxyNode: '', _exitIp: '', _updatedAt: '' }))
    await loadResults()
  } catch {}
}
```

### Step 10：#3 改 AccountTableRows.vue — 加 reason 列

- [ ] **Step 10:** Open `web/src/components/AccountTableRows.vue`. Find the `<el-table-column label="操作"` block. Insert a NEW column IMMEDIATELY BEFORE it:

```vue
    <el-table-column label="原因" min-width="120" show-overflow-tooltip>
      <template #default="{ row }">
        <span v-if="row._reason" :style="row._status === 'deactivated' ? 'color:#f56c6c' : 'color:#909399'">
          {{ row._reason }}
        </span>
        <span v-else style="color:#c0c4cc">-</span>
      </template>
    </el-table-column>
```

### Step 11：#3 改 AccountTableRows.vue — expand-row 顶部 banner

- [ ] **Step 11:** Find the `<el-table-column type="expand">` block. The template inside currently starts with `<div style="font-family:'Consolas'...`. INSERT a banner div IMMEDIATELY BEFORE the existing dark log container:

```vue
    <el-table-column type="expand">
      <template #default="{ row }">
        <!-- v2.28 #3: 顶部 banner — 本次执行的代理上下文 -->
        <div v-if="row._proxyNode || row._exitIp" style="display:flex;gap:16px;padding:8px 12px;background:#f5f7fa;border-radius:4px;margin-bottom:8px;font-size:12px;color:#606266">
          <span v-if="row._proxyNode"><strong>代理节点:</strong> {{ row._proxyNode }}</span>
          <span v-if="row._exitIp"><strong>出口 IP:</strong> {{ row._exitIp }}</span>
          <span v-if="row._updatedAt" style="margin-left:auto;color:#909399">{{ formatTs(row._updatedAt) }}</span>
        </div>
        <!-- 现有黑色日志容器（保持不变） -->
        <div style="font-family:'Consolas','Courier New',monospace;font-size:13px;...">
          ...原内容...
        </div>
      </template>
    </el-table-column>
```

NOTE: 不要复制 "..." — 只是在原有黑色日志容器**之前**插入新的 banner div，原内容保持完全不变。

### Step 12：build + manual smoke

- [ ] **Step 12:**

```bash
cd web && npm run build
```

Expected: Vite build SUCCESS, 无 Vue compile errors.

启动 server 手动 smoke：
```bash
cd .. && node server/index.js &
# 浏览器打开 http://localhost:3000，进 Execute 页
# 验证：
# - toolbar 显示"协议模式/浏览器模式"badge 和"link: API/Discord"badge
# - statusFilter 下拉含 paypal_captcha / login_fail / token_expired / aborted
# - 执行一个账号后看 expand-row：顶部 banner 显示代理节点 + 出口 IP
# - 子表"原因"列显示 reason
# 然后停止 server
```

### Step 13：Commit

- [ ] **Step 13:**

```bash
git add web/src/views/Execute.vue web/src/components/AccountTableRows.vue
git commit -m "$(cat <<'EOF'
feat(execute): 5 项 ops UX 改进 (#1/#2/#3/#4/#5)

#1 failedEmails: a._status === 'error' → isFailedToRetry(a._status)
   涵盖 9 个未拿到 Plus 的可重试终态（之前只识别 error 一个）
#2 statusFilter: 硬编码 11 项 → v-for EXECUTE_STATUS_FILTER_OPTIONS
   动态生成，自动同步 status.js LABEL_MAP 4 个新增状态
#3 子表加"原因"列（reason）+ expand-row 顶部 banner（代理节点 +
   出口 IP + updatedAt）loadResults 透传 4 个新字段
#4 toolbar 加 engineMode badge：协议模式/浏览器模式 + link: API/Discord
#5 5s polling /execute/status 兜底，断 socket 仍可 Stop；
   visibilitychange 暂停节省请求；onBeforeUnmount 清 interval

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5：Accounts.vue 改造（#6/#7/#8）

**Files:**
- Modify: `web/src/views/Accounts.vue`

### Step 1：imports 扩展 + 新方法定义

- [ ] **Step 1:** Open `web/src/views/Accounts.vue`. Find the existing `<script setup>` imports. Add (or extend if already partially present):

- 加 `ElMessageBox` (from element-plus)
- 加 `DocumentCopy` (from @element-plus/icons-vue)
- 加 `EXECUTE_STATUS_FILTER_OPTIONS` (from '../status')

具体替换：原 imports 应类似：

```js
import { ElMessage } from 'element-plus'
```

Replace with:

```js
import { ElMessage, ElMessageBox } from 'element-plus'
import { DocumentCopy } from '@element-plus/icons-vue'
```

如果文件已经从 `'../status'` 导入了任何东西，加 `EXECUTE_STATUS_FILTER_OPTIONS`。否则新加一行：

```js
import { EXECUTE_STATUS_FILTER_OPTIONS } from '../status'
```

在 `<script setup>` 末尾或合适位置添加：

```js
async function copyToClipboard(text, label) {
  try {
    await navigator.clipboard.writeText(text)
    ElMessage.success(`${label} 已复制`)
  } catch {
    ElMessage.error('复制失败（浏览器可能阻止）')
  }
}

async function checkAllWithConfirm() {
  const n = accounts.value.length
  try {
    await ElMessageBox.confirm(
      `将对 ${n} 个账户发起活性检测，可能耗时较长且消耗代理请求配额。继续？`,
      '确认测活全部',
      { type: 'warning', confirmButtonText: '开始测活', cancelButtonText: '取消' },
    )
  } catch { return }
  startLiveness('all')
}
```

### Step 2：#6 重组 toolbar 为 4 行

- [ ] **Step 2:** Replace the entire `<el-row style="margin-bottom: 16px" :gutter="12" align="middle">` block (the giant single row at lines 3-79). Replace with 4 separate `<el-row>` blocks:

```vue
    <!-- v2.28 #6: Row 1 — 数据管理 -->
    <el-row style="margin-bottom: 8px" :gutter="8" align="middle">
      <el-button type="primary" @click="showImport = true">批量导入</el-button>
      <el-button @click="exportAccounts">导出全部</el-button>
      <el-button :disabled="selected.length === 0" @click="exportSelected">导出选中 ({{ selected.length }})</el-button>
      <el-button type="success" @click="openAdd">添加单个</el-button>
      <el-popconfirm :title="`确定删除选中的 ${selected.length} 个账号？`" @confirm="delSelected" v-if="selected.length > 0">
        <template #reference><el-button type="danger" size="small">删除选中 ({{ selected.length }})</el-button></template>
      </el-popconfirm>
    </el-row>

    <!-- v2.28 #6: Row 2 — 筛选 -->
    <el-row style="margin-bottom: 8px" :gutter="8" align="middle">
      <el-input v-model="search" placeholder="搜索 (邮箱/RT/Client ID/TOTP/密码)" clearable style="width:240px" />
      <el-select v-model="statusFilter" placeholder="状态" clearable multiple collapse-tags collapse-tags-tooltip style="width:180px;margin-left:8px">
        <el-option v-for="opt in EXECUTE_STATUS_FILTER_OPTIONS" :key="opt.value" :label="opt.label" :value="opt.value" />
      </el-select>
      <el-select v-model="planFilter" placeholder="Plan" clearable style="width:110px;margin-left:8px">
        <el-option label="Plus" value="plus" />
        <el-option label="Free" value="free" />
        <el-option label="未知" value="unknown" />
      </el-select>
      <el-select v-model="authFilter" placeholder="Auth" clearable style="width:110px;margin-left:8px">
        <el-option label="已生成" value="yes" />
        <el-option label="未生成" value="no" />
      </el-select>
      <el-select v-model="aliveFilter" placeholder="活性" clearable multiple collapse-tags collapse-tags-tooltip size="small" style="width:180px;margin-left:8px">
        <el-option v-for="o in aliveFilterOptions" :key="o.value" :label="o.label" :value="o.value" />
      </el-select>
      <el-button size="small" text @click="aliveFilter = ['unknown']" style="margin-left:8px">仅看未测试</el-button>
      <el-button size="small" :type="staleOnly ? 'primary' : ''" text @click="staleOnly = !staleOnly">7天未测</el-button>
      <el-button size="small" text :disabled="!hasAnyFilter" @click="resetFilters">重置筛选</el-button>
      <el-tag style="margin-left: 8px">{{ filteredAccounts.length }} / {{ accounts.length }}</el-tag>
      <el-button size="small" style="margin-left: 8px" @click="clearAllSelection">取消选中</el-button>
    </el-row>

    <!-- v2.28 #6 + #8: Row 3 — 测活 -->
    <el-row style="margin-bottom: 8px" :gutter="8" align="middle">
      <el-button size="small" type="primary" :disabled="selected.length === 0 || livenessRunning" @click="startLiveness('selected')">
        测活选中 ({{ selected.length }})
      </el-button>
      <el-button size="small" type="primary" :disabled="livenessRunning" @click="checkAllWithConfirm">
        测活全部
      </el-button>
      <el-button v-if="livenessRunning" size="small" type="danger" @click="stopLiveness">
        停止测活
      </el-button>
      <el-tag v-if="livenessRunning" type="info" size="small" style="margin-left: 8px">
        {{ socketState.liveness.done }}/{{ socketState.liveness.total }} (✗{{ socketState.liveness.failed }})
      </el-tag>
    </el-row>

    <!-- v2.28 #6: Row 4 — 下载 -->
    <el-row style="margin-bottom: 12px" :gutter="8" align="middle">
      <el-dropdown :disabled="selected.length === 0" @command="downloadSelectedAs" split-button size="small">
        下载选中 ({{ selected.length }})
        <template #dropdown>
          <el-dropdown-menu>
            <el-dropdown-item command="cpa">CPA 格式</el-dropdown-item>
            <el-dropdown-item command="sub2api">Sub2API 格式</el-dropdown-item>
          </el-dropdown-menu>
        </template>
      </el-dropdown>
      <el-dropdown @command="downloadAllAs" split-button size="small" style="margin-left:8px">
        下载全部 (ZIP)
        <template #dropdown>
          <el-dropdown-menu>
            <el-dropdown-item command="cpa">CPA 格式</el-dropdown-item>
            <el-dropdown-item command="sub2api">Sub2API 格式</el-dropdown-item>
          </el-dropdown-menu>
        </template>
      </el-dropdown>
    </el-row>
```

### Step 3：#7 TOTP / Refresh Token / Client ID 列加 tooltip + 复制按钮

- [ ] **Step 3:** In the `<el-table>` columns, find the TOTP column. Existing should be something like:

```vue
      <el-table-column label="TOTP" width="120">
        <template #default="{ row }">
          <span v-if="!row.totp_secret" style="color:#c0c4cc">-</span>
          <span v-else style="font-family:monospace">{{ row.totp_secret.slice(0, 8) }}...</span>
        </template>
      </el-table-column>
```

Replace with:

```vue
      <el-table-column label="TOTP" width="120">
        <template #default="{ row }">
          <span v-if="!row.totp_secret" style="color:#c0c4cc">-</span>
          <span v-else style="display:inline-flex;align-items:center;gap:4px">
            <el-tooltip :content="row.totp_secret" placement="top">
              <span style="font-family:monospace">{{ row.totp_secret.slice(0, 8) }}...</span>
            </el-tooltip>
            <el-icon style="cursor:pointer;color:#909399" @click="copyToClipboard(row.totp_secret, 'TOTP')">
              <DocumentCopy />
            </el-icon>
          </span>
        </template>
      </el-table-column>
```

Find the Refresh Token column. Apply same pattern (slice length may differ — keep original slice number):

```vue
      <el-table-column label="Refresh Token" width="120">
        <template #default="{ row }">
          <span v-if="!row.refresh_token" style="color:#c0c4cc">-</span>
          <span v-else style="display:inline-flex;align-items:center;gap:4px">
            <el-tooltip :content="row.refresh_token" placement="top">
              <span style="font-family:monospace">{{ row.refresh_token.slice(0, 20) }}...</span>
            </el-tooltip>
            <el-icon style="cursor:pointer;color:#909399" @click="copyToClipboard(row.refresh_token, 'Refresh Token')">
              <DocumentCopy />
            </el-icon>
          </span>
        </template>
      </el-table-column>
```

Find the Client ID column. Apply same pattern:

```vue
      <el-table-column label="Client ID" width="120">
        <template #default="{ row }">
          <span v-if="!row.client_id" style="color:#c0c4cc">-</span>
          <span v-else style="display:inline-flex;align-items:center;gap:4px">
            <el-tooltip :content="row.client_id" placement="top">
              <span style="font-family:monospace">{{ row.client_id.slice(0, 12) }}...</span>
            </el-tooltip>
            <el-icon style="cursor:pointer;color:#909399" @click="copyToClipboard(row.client_id, 'Client ID')">
              <DocumentCopy />
            </el-icon>
          </span>
        </template>
      </el-table-column>
```

NOTE: if the actual existing column code differs (different prop name / slice count), adapt the new code accordingly — keep the original prop name and slice count for visual continuity.

### Step 4：build + manual smoke

- [ ] **Step 4:**

```bash
cd web && npm run build
```

Expected: build SUCCESS.

启动 server 手动 smoke：进 Accounts 页验证：
- toolbar 4 行清晰分组（数据/筛选/测活/下载）
- statusFilter 下拉含 paypal_captcha / login_fail / token_expired / aborted
- TOTP / RT / Client ID 旁边都有复制 icon；hover 显示完整值；点击复制成功 toast
- "测活全部"按钮点击 → 弹 ElMessageBox 显示账户数量 → 取消不发请求

### Step 5：Commit

- [ ] **Step 5:**

```bash
git add web/src/views/Accounts.vue
git commit -m "$(cat <<'EOF'
feat(accounts): toolbar 4 行重组 + token 复制 + 测活二次确认 (#6/#7/#8)

#6 toolbar 16+ 控件单行挤拥 → 拆 4 行 by 职责：
   Row1 数据管理 / Row2 筛选 / Row3 测活 / Row4 下载
   1366 宽屏不再换行；aliveFilter 从下载按钮右边移回筛选行
   statusFilter 改用动态 EXECUTE_STATUS_FILTER_OPTIONS（与 Execute 同源）
#7 TOTP / Refresh Token / Client ID 三列加 el-tooltip 显示完整值 +
   el-icon DocumentCopy 复制按钮，调 navigator.clipboard API +
   ElMessage 反馈
#8 测活全部加 ElMessageBox 二次确认，显示 N 个账户警告，避免
   误点对几千账户的库一键发起测活

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6：Config.vue + CHANGELOG（#9/#10）

**Files:**
- Modify: `web/src/views/Config.vue`
- Modify: `docs/CHANGELOG.md`

### Step 1：Config.vue imports 加 ElMessageBox

- [ ] **Step 1:** Open `web/src/views/Config.vue`. Find existing imports. Ensure `ElMessageBox` is imported (likely already is from previous tasks; if not, add it):

```js
import { ElMessage, ElMessageBox } from 'element-plus'
```

### Step 2：添加 activeTab state

- [ ] **Step 2:** In `<script setup>`, find the existing state declarations. Add:

```js
const activeTab = ref('payment')
```

### Step 3：#9 用 el-tabs 包裹整个 form 内容（7 节）

- [ ] **Step 3:** This is the largest single change. The current Config.vue has all form sections (支付 / 执行 / Discord / OAuth / 代理 / JP / 黑名单) flat inside a single `<el-form>`. Wrap them in `<el-tabs>`.

Find the outer structure (around line 1-20):

```vue
<template>
  <el-card>
    <template #header>
      <div style="display:flex;justify-content:space-between;align-items:center">
        <span>配置设置</span>
        <el-button type="primary" :loading="saving" @click="handleSave">保存配置</el-button>
      </div>
    </template>

    <el-form :model="form" label-width="160px" style="max-width:600px">
      <!-- 全部表单内容 -->
      ...
    </el-form>
  </el-card>
</template>
```

Replace the outer `<el-form>` block with an `<el-tabs>` containing 7 `<el-tab-pane>`s, each containing the form sections currently belonging to that area. The mapping:

| Tab | name | 包含原内容 |
|---|---|---|
| 支付 | payment | 手机号 / 短信 API URL |
| 执行 | execute | 协议注册模式 / 支付链接来源 |
| Discord | discord | discordToken / channelId / messageId / guildId / appId |
| OAuth / CPA | oauth | enableOAuth / enableCPA / cpaUrl / cpaKey |
| 代理 | proxy | 主代理：订阅 URL / 节点白名单 / 区域过滤 / 轮换策略 / 启动按钮 / 状态卡 |
| JP 通道 | jp | JP 启用 / 关键字 / 白名单 / JP 状态卡 |
| 节点黑名单 | blacklist | 主代理黑名单表格 + JP 黑名单表格 |

完整替换结构：

```vue
<template>
  <el-card>
    <template #header>
      <div style="display:flex;justify-content:space-between;align-items:center">
        <span>配置设置</span>
        <el-button type="primary" :loading="saving" @click="handleSave">保存配置</el-button>
      </div>
    </template>

    <el-tabs v-model="activeTab" type="border-card">
      <el-tab-pane label="支付" name="payment">
        <el-form :model="form" label-width="160px" style="max-width:600px">
          <!-- 此处放原"支付配置"分节的 el-form-item：手机号 / 短信 API URL -->
        </el-form>
      </el-tab-pane>

      <el-tab-pane label="执行" name="execute">
        <el-form :model="form" label-width="160px" style="max-width:600px">
          <!-- 此处放原"执行模式"分节：协议注册模式 / 支付链接来源 -->
        </el-form>
      </el-tab-pane>

      <el-tab-pane label="Discord" name="discord">
        <el-form :model="form" label-width="160px" style="max-width:600px">
          <!-- 此处放原"Discord 配置"分节：discordToken / channelId / messageId / guildId / appId 5 个字段 -->
        </el-form>
      </el-tab-pane>

      <el-tab-pane label="OAuth / CPA" name="oauth">
        <el-form :model="form" label-width="160px" style="max-width:600px">
          <!-- 此处放原"OAuth/CPA 配置"分节：enableOAuth / enableCPA / cpaUrl / cpaKey -->
        </el-form>
      </el-tab-pane>

      <el-tab-pane label="代理" name="proxy">
        <el-form :model="form" label-width="160px" style="max-width:600px">
          <!-- 此处放原"代理 / 节点轮换"分节：主代理订阅 URL / 节点白名单 / 区域过滤 / 轮换策略 / 启动按钮 / 状态卡 -->
        </el-form>
      </el-tab-pane>

      <el-tab-pane label="JP 通道" name="jp">
        <el-form :model="form" label-width="160px" style="max-width:600px">
          <!-- 此处放原"JP-Checkout 通道"分节：JP 启用 / 关键字 / 白名单 / JP 状态卡 -->
        </el-form>
      </el-tab-pane>

      <el-tab-pane label="节点黑名单" name="blacklist">
        <!-- 注意：黑名单 max-width 突破限制，直接放表格不包 el-form -->
        <!-- 此处放原"节点黑名单"分节：主代理黑名单表格 + JP 黑名单表格 -->
      </el-tab-pane>
    </el-tabs>
  </el-card>
</template>
```

**Important manual step**: 把原 `<el-form>` 内的每个 `<el-form-item>` 按上述映射 cut/paste 到对应 `<el-tab-pane>` 内的新 `<el-form>` 块。所有 v-model 绑定保持不变（仍指向同一个 `form` reactive object）。

**注意**：el-tabs `type="border-card"` 默认 always-render，所有 v-model 字段始终存在，保存按钮仍是全局一个。

### Step 4：#10 停止代理二次确认

- [ ] **Step 4:** Find the `stopProxy` function:

```js
async function stopProxy() {
  try {
    await api.post('/proxy/stop')
    ElMessage.success('代理已停止')
    await loadProxyStatus()
  } catch (err) {
    ElMessage.error(err.response?.data?.error || '停止失败')
  }
}
```

Replace with:

```js
async function stopProxy() {
  try {
    await ElMessageBox.confirm(
      '停止代理将断开当前所有 execute / liveness 流水线的网络。确认停止？',
      '确认停止代理',
      { type: 'warning', confirmButtonText: '停止', cancelButtonText: '取消' },
    )
  } catch { return }
  try {
    await api.post('/proxy/stop')
    ElMessage.success('代理已停止')
    await loadProxyStatus()
  } catch (err) {
    ElMessage.error(err.response?.data?.error || '停止失败')
  }
}
```

### Step 5：build + manual smoke

- [ ] **Step 5:**

```bash
cd web && npm run build
```

Expected: build SUCCESS.

启动 server smoke：进 Config 页验证：
- 7 个 tab 切换流畅；每个 tab 显示原应有字段
- 改任一字段切到其他 tab 再切回，值保留
- "保存配置"按钮仍是全局一个；保存后所有 tab 字段都持久化
- "停止代理"点击 → 弹 ElMessageBox → 取消不停；确认才停

### Step 6：CHANGELOG 加 v2.28 节

- [ ] **Step 6:** Open `docs/CHANGELOG.md`. Find the very top (line 1):

```markdown
# Changelog
```

Insert the new v2.28 section IMMEDIATELY AFTER this heading and BEFORE the most recent existing version section:

```markdown

## v2.28.0 — 2026-05-25

### Ops UX Fixes — 10 个 P1 改进一次性 ship

3 模块审查（执行控制 / 账号管理 / 代理）共发现 32 个改进项，
10 个 P1 打包本版本一次性 ship。覆盖 v2.18-v2.32 快速迭代积攒
的 3 类债：(a) 状态维度扩展未同步到 UI、(b) 后端能力 > 前端
暴露、(c) 危险操作无守卫 + UI 体力活。

**Execute 页（5 项）：**

- **#1 `failedEmails` 涵盖所有可重试终态（9 个）**：之前只识别
  `error` 一个，"重试失败"按钮严重失真；改用新 helper
  `isFailedToRetry()` 涵盖 error / no_link / no_promo /
  verify_error / paypal_captcha / login_fail / token_expired /
  aborted / no_jp_proxy 共 9 个未拿到 Plus 但非死号的终态。
- **#2 statusFilter 下拉动态生成**：从硬编码 11 项 → v-for
  `EXECUTE_STATUS_FILTER_OPTIONS`（status.js LABEL_MAP 全集除
  checking / canceled），下拉自动与状态体系同步。
- **#3 子表加 `原因` 列 + expand-row 顶部 banner**：reason 上列
  高频可见；代理节点 + 出口 IP + updatedAt 放 banner 低频查阅。
  后端 account_status 加 proxy_node / exit_ip 两列 + engine
  emitStatus 时从 proxyMgr 注入。
- **#4 toolbar 显示 engine mode badge**：协议模式 / 浏览器模式 +
  link: API / Discord，从 /config/raw 读 protocolMode 和
  paymentLinkSource。
- **#5 5s polling /execute/status 兜底 Stop 按钮**：socket 仍是
  fast path，polling 仅作为冗余；visibilitychange 暂停节省请求。
  断 socket 后仍可成功 Stop 引擎。

**Accounts 页（3 项）：**

- **#6 toolbar 拆 4 行**：16+ 控件由单行挤拥改成 by 职责分组
  （数据管理 / 筛选 / 测活 / 下载）；aliveFilter 从下载按钮右
  边移回筛选行；statusFilter 改用 EXECUTE_STATUS_FILTER_OPTIONS。
- **#7 TOTP / Refresh Token / Client ID 列加 tooltip + 复制**：
  el-tooltip 显示完整值；DocumentCopy icon 调 navigator.clipboard
  复制并 ElMessage 反馈。
- **#8 测活全部加 ElMessageBox 二次确认**：显示账户数量警告，避
  免误点对几千账户的库一键发起测活打爆 API/proxy。

**Config 页（2 项）：**

- **#9 整页用 7 个 el-tabs 重构**：支付 / 执行 / Discord / OAuth /
  代理 / JP / 节点黑名单。原 482 行平铺由 tab 切换替代滚动查找；
  保存按钮仍是全局一个（所有 tab 字段同时保存）。
- **#10 停止代理加 ElMessageBox 二次确认**：警告将断开当前所有
  execute / liveness 流水线的网络。

**后端配套：**

- `account_status` 表加 `proxy_node` / `exit_ip` 两列；CREATE
  TABLE 块同步 + 老库 PRAGMA-gated ALTER 防御性迁移（幂等不抛错）。
- `statusDB.set` 沿用既有 'in incoming' 显式判断 merge-aware
  pattern（与 paymentLink / accessToken 同），中间态 emitStatus
  不会清空已写入的代理上下文。
- `server/engine.js` + `protocol-engine.js` emitStatus 时从
  `proxyMgr.getState()` 读 currentNode + exitIp 注入 data。
- `/api/results` 透传 proxyNode / exitIp camelCase 字段。

**单测**：6 个新增 — `db-status-proxy-cols.test.js` +3 (M1-M3) +
`status-groups.test.js` 追加 G11/G12 + `status-filter-options.test.js`
新建 S1。无回归。

**Spec / Plan**：
`docs/superpowers/specs/2026-05-25-v2.28-ops-ux-fixes-design.md` +
`docs/superpowers/plans/2026-05-25-v2.28-ops-ux-fixes.md`。

```

### Step 7：跑全套测试做最终冒烟

- [ ] **Step 7:**

```bash
node --test __tests__/*.test.js server/__tests__/*.test.js server/proxy/__tests__/*.test.js server/liveness/__tests__/*.test.js
py -3 -m unittest tests.test_protocol_register_h1_fallback
```

Expected: 全 PASS（既有 184 + 新 6 = 190 个 JS + 4 个 Python）

### Step 8：Commit

- [ ] **Step 8:**

```bash
git add web/src/views/Config.vue docs/CHANGELOG.md
git commit -m "$(cat <<'EOF'
feat(config): 7 tab 重构 + 停止代理二次确认 + v2.28 CHANGELOG (#9/#10)

#9 Config 482 行平铺 form → el-tabs 7 节（支付 / 执行 / Discord /
   OAuth / 代理 / JP / 节点黑名单）；type="border-card" 默认
   always-render，所有 v-model 字段同时存在，保存按钮仍是全局
   一个（不拆"按 tab 保存"以降低复杂度）。activeTab='payment'
   默认打开。
#10 停止代理加 ElMessageBox 二次确认，警告将断开当前所有
    execute / liveness 流水线网络。

docs/CHANGELOG.md 追加 v2.28.0 节，含 3 模块 10 项 P1 全量说明
+ 后端配套（schema migration + engine 注入 + results 透传） +
单测覆盖 + Spec/Plan 引用。

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## 全套测试一遍（实施完成后最终冒烟）

- [ ] 6 任务完成后：

```bash
node --test __tests__/*.test.js server/__tests__/*.test.js server/proxy/__tests__/*.test.js server/liveness/__tests__/*.test.js
py -3 -m unittest tests.test_protocol_register_h1_fallback
```

Expected: 190/190 JS PASS + 4/4 Python PASS = **194/194**

- [ ] **人工 14 项验证清单**（spec §测试策略·人工验证清单）跑完
